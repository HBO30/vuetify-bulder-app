import Vue from 'vue'
import Vuex from 'vuex'
import Core from '../modules/core/store'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Core
  }
})
